let neighbors: string[] = ["NodeA", "NodeB", "NodeC"];

println("Iterating through neighbors:");
for (const neighbor of neighbors) {
    println(neighbor);
}

let numbers: i32[] = [10, 20, 30];
println("Iterating through numbers:");
for (let num of numbers) {
    println(num);
}
